import { StaticPage } from "@/components/layout/StaticPage";

export default function EstimateGeneratorPage() {
  return (
    <StaticPage title="Estimate Generator">
      <p>Create estimate documents with the same live builder and export flow.</p>
    </StaticPage>
  );
}
