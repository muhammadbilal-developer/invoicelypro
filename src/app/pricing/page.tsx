import { StaticPage } from "@/components/layout/StaticPage";

export default function PricingPage() {
  return (
    <StaticPage title="Pricing">
      <p>Core invoice generation is free. Pro features will include advanced automation and team workflows.</p>
    </StaticPage>
  );
}
