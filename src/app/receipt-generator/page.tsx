import { StaticPage } from "@/components/layout/StaticPage";

export default function ReceiptGeneratorPage() {
  return (
    <StaticPage title="Receipt Generator">
      <p>Create printable receipt documents quickly with no signup required.</p>
    </StaticPage>
  );
}
